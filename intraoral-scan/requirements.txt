# Core (required for SSM completion + validation metrics + tests)
numpy>=1.24
scipy>=1.10

# Capture / calibration / reconstruction (optional, install as needed)
opencv-contrib-python>=4.7   # video decode + ArUco/ChArUco scale targets
trimesh>=4.0                 # mesh I/O and sampling
open3d>=0.17                 # point-cloud / ICP / visualisation (optional)

# Reconstruction engines are external binaries / repos, not pip deps:
#   COLMAP            -> https://colmap.github.io/        (SfM + MVS)
#   gaussian-splatting-> https://github.com/graphdeco-inria/gaussian-splatting
#
# Deep models for segmentation / completion (install per your chosen backbone):
#   torch, torch-geometric (TeethGNN / point transformers on Teeth3DS)

# Dev
pytest>=7.0
